route:
  receiver: equipe-potager
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: equipe-potager
    # Remplacer par le webhook Slack ou Discord de l'equipe
    webhook_configs:
      - url: "https://exemple.invalid/webhook"
        send_resolved: true
